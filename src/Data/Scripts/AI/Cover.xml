<Cover>
	<DynamicSurfaceEntities>
		<EntityClass name="BreakableObject" />
		<!-- CIG Gordon McLean: The below classes will not be registered correctly
		in CCoverSystem::ReloadConfig due to it getting called prior to
		CGameFactory::InitGameFactory. As dynamic cover is not currently used I have
		removed these lines to get rid of the errors in the log. See RSI-54216. -->
		<!--<EntityClass name="DestroyableChunk" />
		<EntityClass name="DestroyableFrame" />
		<EntityClass name="DestroyablePiece" />
		<EntityClass name="DestroyableProp" />-->
	</DynamicSurfaceEntities>
</Cover>